# Dependencies
node_modules/

# 2.4GB of local photo material (personal originals, foldered by city) stays out of the repo;
# web-image thumbnails photos/_thumbs (landmark web images, 92MB) + the manifest data/webimages.json
# are committed and deployed so the map cards have images (user decision, 2026-07-11)
photos/*
!photos/_thumbs/

# System / tool artifacts
.DS_Store
.playwright-mcp/
.claude/
__pycache__/
# Feedback screenshots dropped into the root during design sessions — never commit
Screenshot*.png

# Hard-link folders used for photo triage (local only)
_sorted/
