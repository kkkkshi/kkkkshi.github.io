{
  "name": "footprints",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "live-server --port=1999 --ignorePattern='\\.playwright-mcp|\\.DS_Store'"
  },
  "devDependencies": {
    "http-server": "^14.1.1",
    "live-server": "^1.2.2"
  }
}
